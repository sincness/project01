import { CookieDirective } from './cookie.directive';

describe('CookieDirective', () => {
  it('should create an instance', () => {
    const directive = new CookieDirective();
    expect(directive).toBeTruthy();
  });
});
